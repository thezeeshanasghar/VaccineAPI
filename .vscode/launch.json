{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Launch (web)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",  // Replace if you have a different build task name
            "program": "${workspaceFolder}/bin/Debug/net6.0/VaccineAPI.dll",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "console": "internalConsole",
            "requireExactSource": true,
            "env": {
                "ASPNETCORE_ENVIRONMENT" : "Development",

            
    "DefaultDBConnection": "server=localhost;userid=root;password='';database=fernfers_vaccinebe;port=3306;"
  
            }
        }
    ]
}
