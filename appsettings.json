{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "LinkLogin": {
    "_comment": "Secret used to sign parent magic-link tokens. Set a strong random value on prod (or the LinkLoginSecret env var). Rotating it invalidates all outstanding links.",
    "Secret": "CHANGE_ME_link_login_signing_secret"
  }
}
